name: Build Number to 42

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Set build number
        run: echo "Build number $GITHUB_RUN_NUMBER"